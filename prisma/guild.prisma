model Guild {
  guildId       String   @id
  language      String   @default("en")
  botChannelId  String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  playlists     Music[]
}